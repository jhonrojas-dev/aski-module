<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <!-- Marca Aski: el ICONO REAL de la app, compuesto igual que el adaptive-icon
         de Android (bg #152642 + foreground con inset 16%, recorte 72/108). Se
         enmascara con squircle (border-radius 26%) en el SCSS, NO con circulo.
         OJO: el ?v= es OBLIGATORIO — Odoo/el service worker cachean los estaticos
         por URL, asi que cambiar el PNG sin cambiar la URL NO se ve (ni con
         Ctrl+Shift+R). Si se vuelve a tocar el PNG, SUBIR el numero. -->
    <t t-name="aski_connector.AskiMark" owl="1">
        <img class="ac-mark-img" src="/aski_connector/static/src/img/aski_mark.png?v=4" alt="Aski"/>
    </t>

    <!-- La linea de "a que conexiones cubre este aviso", compartida por el
         resumen diario y el cierre del dia. `fijo` es el aviso, o nada si aun no
         existe (entonces no se pinta: no hay nada que cubrir todavia). -->
    <t t-name="aski_connector.AskiInsightConns" owl="1">
        <small t-if="fijo" class="ac-ins-conns">
            <i class="fa fa-database"/>
            <span class="ac-ins-conns-n" t-esc="etiquetaConexiones(fijo)"/>
            <span t-if="cubreVarias(fijo)" class="ac-ins-conns-multi"
                  t-esc="txt.connMany"/>
            <button t-if="hayVariasConexiones" class="ac-ins-conns-edit"
                    t-on-click="abrirConexiones(fijo)"
                    t-esc="txt.connChange"/>
        </small>
        <small t-if="fijo and dejaFueraEsteOdoo(fijo)" class="ac-ins-conns-warn"
               t-esc="txt.connNotHere"/>
    </t>

    <t t-name="aski_connector.ChatWidget" owl="1">
        <div class="aski-chat">

            <div class="ac-header" t-att-class="props.minimized ? 'ac-header-clickable' : ''"
                 t-on-click="onHeaderClick()">
                <div class="ac-header-l">
                    <button t-if="state.connected and !props.minimized" class="ac-icon-btn" title="Conversations"
                            t-on-click.stop="toggleDrawer">
                        <i class="fa fa-bars"/>
                    </button>
                    <span class="ac-mark">
                        <t t-call="aski_connector.AskiMark"/>
                        <span t-if="state.connected" class="ac-mark-dot"/>
                    </span>
                    <div class="ac-header-id">
                        <span class="ac-title">Aski</span>
                        <span t-if="!props.minimized" class="ac-subtitle">AI assistant · read-only</span>
                    </div>
                </div>
                <div class="ac-header-r">
                    <!-- La campana SOLO a pantalla completa. En la burbuja del
                         systray la cabecera ya lleva marca, creditos, plan,
                         minimizar y cerrar: ahi no cabe y se saco. A pantalla
                         completa sobra sitio y es donde se la busca.
                         ⛔ VA PRIMERA, antes del nombre de la conexion: es un
                         BOTON, y el resto de la fila (nombre, saldo, plan) son
                         etiquetas. Metido en medio de ellas se leia como una mas
                         y dejaba de invitar a pulsarlo. -->
                    <button t-if="state.connected and !enBurbuja"
                            class="ac-icon-btn ac-header-bell" title="Scheduled alerts"
                            t-on-click.stop="openInsights">
                        <!-- La campana MACIZA, no la de contorno: dentro de su
                             pill, el trazo fino de `fa-bell-o` se perdia contra
                             el fondo y no pesaba lo mismo que el saldo o el plan
                             de al lado. `fa-bell` existe desde FontAwesome 4, que
                             es la que traen las seis series. -->
                        <i class="fa fa-bell"/>
                    </button>
                    <!-- QUE conexion se esta consultando. Una cuenta Aski puede
                         tener varias instancias colgando (produccion, pruebas,
                         la filial) y sin este nombre dos chats abiertos se ven
                         exactamente igual. Se oculta minimizado, donde no cabe;
                         el nombre completo queda en el `title`. -->
                    <span t-if="state.connected and state.connectionName and !props.minimized"
                          class="ac-conn" t-att-title="tituloConexion">
                        <i class="fa fa-database"/>
                        <span class="ac-conn-n" t-esc="state.connectionName"/>
                    </span>
                    <t t-if="state.connected">
                        <button t-if="state.walletCredits === 0 and !state.partnerManaged"
                                class="ac-credits ac-credits-empty"
                                title="Out of credits - tap to top up" t-on-click.stop="openBilling">
                            <i class="fa fa-exclamation-circle"/> Out of credits
                        </button>
                        <!-- ⛔ `t-elif`, NO un segundo `t-if`. El `t-else` de
                             abajo se empareja con el `t-if` que tiene JUSTO
                             encima: con dos `t-if` sueltos, a un cliente SIN
                             socio y con saldo 0 le salian las DOS pildoras a la
                             vez —"Sin creditos" y "0"— porque el segundo `t-if`
                             era falso y disparaba su `t-else`. Visto en una
                             instancia real de QA. Asi la cadena es una sola:
                             sin saldo y sin socio, sin saldo con socio, o saldo. -->
                        <span t-elif="state.walletCredits === 0 and state.partnerManaged"
                              class="ac-credits ac-credits-empty"
                              title="Out of credits - your Aski partner tops them up">
                            <i class="fa fa-exclamation-circle"/> Out of credits
                        </span>
                        <span class="ac-credits" t-else="">
                            <i class="fa fa-bolt"/> <t t-esc="creditosFmt"/>
                        </span>
                        <!-- El nombre del plan, SOLO a pantalla completa. En la
                             burbuja no aporta nada —el saldo si— y ademas empuja
                             a la marca contra el borde. -->
                        <span class="ac-plan"
                              t-if="state.planName and !props.minimized and !enBurbuja"
                              t-esc="state.planName"/>
                    </t>
                    <button t-if="props.onMinimize" class="ac-icon-btn" title="Minimize"
                            t-on-click.stop="onMinimizeClick()">
                        <i class="fa fa-minus"/>
                    </button>
                    <button t-if="props.onClose" class="ac-icon-btn ac-icon-btn-close" title="Close"
                            t-on-click.stop="onCloseClick()">
                        <i class="fa fa-times"/>
                    </button>
                </div>
            </div>

            <t t-if="!props.minimized">

            <!-- Drawer de conversaciones, estilo Discuss/ChatGPT -->
            <t t-if="state.drawerOpen">
                <div class="ac-scrim" t-on-click="toggleDrawer"/>
                <div class="ac-drawer">
                    <div class="ac-drawer-head">Conversations</div>
                    <button class="ac-drawer-new" t-on-click="newConversation">
                        <i class="fa fa-plus"/> New conversation
                    </button>


                    <!-- Buscar DENTRO de los mensajes. Va aqui, pegado a la
                         lista, porque filtra lo que hay debajo y no lo de
                         arriba. Los textos salen de `txt` (JS) y no como
                         literales: en 14 y 15 el texto estatico de una
                         plantilla OWL no se traduce nunca. -->
                    <div class="ac-drawer-search">
                        <i class="fa fa-search"/>
                        <input type="text" class="ac-drawer-search-input"
                               t-att-value="state.searchQ" t-on-input="onSearchInput"
                               t-att-placeholder="txt.searchPh"
                               t-att-aria-label="txt.searchPh"/>
                        <button t-if="state.searchQ" class="ac-drawer-search-x"
                                t-att-title="txt.searchClear"
                                t-att-aria-label="txt.searchClear"
                                t-on-click="clearSearch">
                            <i class="fa fa-times"/>
                        </button>
                    </div>

                    <!-- SOLO la lista scrollea. Con el cajon entero scrolleando,
                         veinte hilos empujaban fuera de pantalla la cuenta y
                         «Desconectar»: habia que bajar hasta el fondo para
                         encontrarlos, y los destinos de arriba desaparecian. -->
                    <div class="ac-drawer-list">
                    <!-- Con busqueda activa el cajon ensena RESULTADOS en vez
                         de hilos: los cuatro estados, sin dejar ninguno mudo. -->
                    <t t-if="searchActive">
                        <div t-if="state.searchLoading" class="ac-drawer-empty">
                            <i class="fa fa-circle-o-notch fa-spin"/> <t t-esc="txt.searching"/>
                        </div>
                        <div t-elif="state.searchCode" class="ac-drawer-empty ac-drawer-searcherr">
                            <div t-esc="searchErrTxt"/>
                            <button class="ac-btn-link" t-on-click="retrySearch"
                                    t-esc="txt.retry"/>
                        </div>
                        <div t-elif="!state.searchResults.length" class="ac-drawer-empty"
                             t-esc="searchEmptyTxt"/>
                        <t t-else="">
                            <t t-foreach="state.searchResults" t-as="r" t-key="r.messageId">
                                <button class="ac-search-item"
                                        t-on-click="openSearchResult(r)">
                                    <span class="ac-search-conv" t-esc="searchConvTitle(r)"/>
                                    <!-- El fragmento va en TRES trozos de texto y
                                         nunca como HTML: es texto del usuario y de
                                         sus datos. Los offsets del resaltado los
                                         calcula el backend (ver snippetPartes). -->
                                    <span class="ac-search-snip"><t t-esc="snippetPartes(r).pre"/><mark t-esc="snippetPartes(r).hit"/><t t-esc="snippetPartes(r).post"/></span>
                                    <span class="ac-search-date" t-esc="searchFecha(r)"/>
                                </button>
                            </t>
                        </t>
                    </t>
                    <t t-if="!searchActive">
                    <div t-if="!state.conversations.length" class="ac-drawer-empty">
                        No conversations yet.
                    </div>
                    <t t-foreach="state.conversations" t-as="c" t-key="c.id">
                        <!-- Renombrando ESTE hilo -->
                        <div t-if="state.renamingId === c.id" class="ac-drawer-rename">
                            <input type="text" t-att-value="state.renameText"
                                   t-on-input="onRenameInput" t-on-keydown="onRenameKeydown"
                                   placeholder="Conversation name"/>
                            <div class="ac-confirm-actions">
                                <button class="ac-confirm-cancel" t-on-click="cancelRename">Cancel</button>
                                <button class="ac-confirm-ok" t-on-click="doRename"
                                        t-att-disabled="!state.renameText.trim()">Save</button>
                            </div>
                        </div>

                        <!-- Confirmando el borrado de ESTE hilo. In-app, nunca
                             window.confirm. Se dice "quitar de la lista" porque
                             del lado de Aski es un archivado, no un borrado. -->
                        <div t-elif="state.confirmDeleteId === c.id" class="ac-confirm ac-confirm-inline">
                            <div class="ac-confirm-txt">Remove this conversation from your list?</div>
                            <div class="ac-confirm-actions">
                                <button class="ac-confirm-cancel" t-on-click="cancelDelete">Cancel</button>
                                <button class="ac-confirm-ok" t-on-click="doDelete">Remove</button>
                            </div>
                        </div>

                        <div t-else="" t-attf-class="ac-drawer-item {{ c.id === state.conversationId ? 'active' : '' }}">
                            <span class="ac-drawer-title" t-on-click="openConversation(c.id)"
                                  t-esc="conversationTitle(c)"/>
                            <span class="ac-drawer-item-actions">
                                <button class="ac-icon-btn" title="Rename"
                                        t-on-click.stop="askRename(c)">
                                    <i class="fa fa-pencil"/>
                                </button>
                                <button class="ac-icon-btn" title="Remove"
                                        t-on-click.stop="askDelete(c)">
                                    <i class="fa fa-trash-o"/>
                                </button>
                            </span>
                        </div>
                    </t>
                    </t>
                    </div>


                    <!-- Destinos FIJOS, al pie y siempre a la vista: el cajon ya
                         no scrollea (solo la lista), asi que no se los lleva el
                         historial por largo que sea. -->
                    <div t-if="state.connected" class="ac-drawer-dest">
                        <button class="ac-drawer-insights" t-on-click="openInsights">
                            <i class="fa fa-bell-o"/> Scheduled alerts
                        </button>
                        <button class="ac-drawer-insights ac-drawer-team" t-on-click="openTeam">
                            <i class="fa fa-users"/> My team
                        </button>
                        <button class="ac-drawer-insights ac-drawer-cat"
                                t-on-click="abrirCatalogo('action')">
                            <i class="fa fa-magic"/> What I can do
                        </button>
                    </div>

                    <div t-if="state.connected and state.canConnect" class="ac-drawer-foot">
                        <!-- CON QUE cuenta se esta hablando. En modo "por
                             usuario" cada quien conecta la suya y, sin esto, no
                             habia forma de saber cual quedo conectada aqui. -->
                        <div t-if="state.email" class="ac-drawer-account">
                            <i class="fa fa-user-circle-o"/> <t t-esc="state.email"/>
                        </div>
                        <button t-if="!state.confirmDisconnect" class="ac-drawer-disconnect"
                                t-on-click="askDisconnect">
                            <i class="fa fa-sign-out"/> Disconnect my Aski account
                        </button>
                        <div t-else="" class="ac-confirm">
                            <div class="ac-confirm-txt">Disconnect this Aski account? You can reconnect anytime with your token.</div>
                            <div class="ac-confirm-actions">
                                <button class="ac-confirm-cancel" t-on-click="cancelDisconnect">Cancel</button>
                                <button class="ac-confirm-ok" t-on-click="doDisconnect"
                                        t-att-disabled="state.disconnecting">
                                    <i t-if="state.disconnecting" class="fa fa-circle-o-notch fa-spin"/>
                                    Disconnect
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </t>


            <!-- ==========================================================
                 Grafico. En una VENTANA y no dentro de la burbuja, por lo
                 mismo que en la app: la tabla se queda siempre visible —es
                 lo auditable— y el mensaje no cambia de tamano al abrirlo.
                 Sin librerias: SVG a mano. Meter una dependencia entera
                 para tres formas no se justifica.
                 ========================================================== -->
            <t t-if="chartAbierto">
                <div class="ac-win-wrap">
                    <div class="ac-win-scrim" t-on-click="closeChart"/>
                <div class="ac-win ac-win-chart" role="dialog" aria-modal="true">
                    <div class="ac-win-head">
                        <span class="ac-win-title">Chart</span>
                        <button class="ac-icon-btn" title="Close" t-on-click="closeChart">
                            <i class="fa fa-times"/>
                        </button>
                    </div>
                    <div class="ac-win-body">
                        <t t-foreach="chartAbierto.series" t-as="s" t-key="s.currency + '-' + s_index">
                            <section class="ac-chart-serie">
                                <!-- El encabezado de moneda SOLO con varias: con
                                     una sola seria ruido, y el simbolo ya va en
                                     cada cifra. Jamas se comparten ejes entre
                                     monedas. -->
                                <h4 t-if="chartAbierto.series.length > 1 and s.currency"
                                    class="ac-chart-cur" t-esc="s.currency"/>

                                <!-- Barras -->
                                <div t-if="chartAbierto.kind === 'bar'" class="ac-chart-bars">
                                    <t t-foreach="barras(s, chartAbierto)" t-as="f" t-key="f.label + '-' + f_index">
                                        <div t-attf-class="ac-bar-row {{ f.otros ? 'otros' : '' }}">
                                            <span class="ac-bar-lbl" t-att-title="f.label" t-esc="f.label"/>
                                            <span class="ac-bar-track">
                                                <span t-if="!f.otros" class="ac-bar-fill"
                                                      t-attf-style="width: {{ f.pct }}%"/>
                                            </span>
                                            <span class="ac-bar-val" t-esc="f.valor"/>
                                        </div>
                                    </t>
                                </div>

                                <!-- Linea -->
                                <t t-if="chartAbierto.kind === 'line'">
                                    <t t-set="L" t-value="linea(s, chartAbierto)"/>
                                    <div class="ac-chart-line">
                                        <div class="ac-line-axis">
                                            <span t-esc="L.arriba"/>
                                            <span t-esc="L.abajo"/>
                                        </div>
                                        <svg t-attf-viewBox="0 0 {{ L.W }} {{ L.H }}"
                                             preserveAspectRatio="none" class="ac-line-svg" role="img">
                                            <t t-foreach="L.guias" t-as="g" t-key="g_index">
                                                <line x1="0" t-att-x2="L.W" t-att-y1="g" t-att-y2="g"
                                                      class="ac-line-grid"/>
                                            </t>
                                            <!-- ⛔ SIN puntos en los extremos: el
                                                 lienzo se estira en horizontal y eso
                                                 convierte un circulo en una elipse
                                                 aplastada que parece un fallo de
                                                 dibujo. El trazo se salva con
                                                 vector-effect, un circle no. -->
                                            <path t-att-d="L.d" class="ac-line-path"/>
                                        </svg>
                                        <div class="ac-line-lbls">
                                            <span t-esc="L.primero"/>
                                            <span t-esc="L.ultimo"/>
                                        </div>
                                    </div>
                                </t>

                                <!-- Dona -->
                                <t t-if="chartAbierto.kind === 'donut'">
                                    <t t-set="D" t-value="dona(s)"/>
                                    <div t-if="D" class="ac-chart-donut">
                                        <svg viewBox="0 0 180 180" class="ac-donut-svg" role="img">
                                            <g transform="translate(90,90) rotate(-90)">
                                                <t t-foreach="D.trozos" t-as="z" t-key="z_index">
                                                    <circle t-att-r="D.R" fill="none" t-att-stroke-width="D.G"
                                                            t-attf-class="ac-slice {{ z.clase }}"
                                                            t-att-stroke-dasharray="z.dash"
                                                            t-att-stroke-dashoffset="z.offset"/>
                                                </t>
                                            </g>
                                        </svg>
                                        <ul class="ac-donut-legend">
                                            <t t-foreach="D.trozos" t-as="z" t-key="z_index">
                                                <li>
                                                    <i t-attf-class="ac-dot {{ z.clase }}"/>
                                                    <span class="ac-leg-lbl" t-att-title="z.label" t-esc="z.label"/>
                                                    <span class="ac-leg-pct"><t t-esc="z.pct"/>%</span>
                                                </li>
                                            </t>
                                        </ul>
                                    </div>
                                </t>

                                <p class="ac-chart-foot" t-esc="pieGrafico(s, chartAbierto)"/>
                            </section>
                        </t>
                    </div>
                </div>
                </div>
            </t>

            <!-- ==========================================================
                 La biblioteca: TODO lo que esta conexion sabe responder,
                 agrupado por seccion. Las secciones y las preguntas llegan ya
                 traducidas del backend, asi que aqui no se traduce contenido:
                 solo el marco. Reusa `.ac-modal tall` en vez de inventar otro
                 dialogo — su cuerpo ya hace scroll con la cabecera fija, que es
                 justo lo que pide una lista larga.
                 ========================================================== -->
            <t t-if="state.libraryOpen and startSections.length">
                <div class="ac-modal-wrap">
                    <div class="ac-modal-scrim" t-on-click="closeLibrary"/>
                    <div class="ac-modal tall" role="dialog" aria-modal="true">
                        <span class="ac-modal-badge"><i class="fa fa-list-ul"/></span>
                        <h3 class="ac-modal-title" t-esc="txt.libTitle"/>
                        <div class="ac-modal-body">
                            <t t-foreach="startSections" t-as="sec" t-key="sec.key">
                                <div class="ac-lib-sec" t-esc="sec.label"/>
                                <button t-foreach="sec.questions" t-as="q" t-key="q.key"
                                        class="ac-lib-q" t-esc="q.text"
                                        t-on-click="useLibraryQuestion(q)"/>
                            </t>
                        </div>
                        <div class="ac-modal-acts">
                            <button class="ac-btn ghost" t-on-click="closeLibrary"
                                    t-esc="txt.close"/>
                        </div>
                    </div>
                </div>
            </t>

            <!-- ==========================================================
                 Compartir enlace. Solo CREA: administrar los enlaces de la
                 cuenta (verlos, revocarlos) no se hace desde el Odoo de un
                 cliente, y el backend tampoco lo acepta con token personal.
                 ========================================================== -->
            <t t-if="state.shareFor">
                <div class="ac-modal-wrap">
                    <div class="ac-modal-scrim" t-on-click="closeShare"/>
                    <div class="ac-modal" role="dialog" aria-modal="true">
                        <span class="ac-modal-badge"><i class="fa fa-link"/></span>
                        <t t-if="!state.shareUrl">
                            <h3 class="ac-modal-title">Share link</h3>
                            <p class="ac-modal-sub">Anyone with the link can read this answer. It expires on its own.</p>
                            <div class="ac-modal-body">
                                <span class="ac-modal-label">How long it lasts</span>
                                <div class="ac-modal-chips">
                                    <button t-foreach="[7, 30]" t-as="d" t-key="d"
                                            t-attf-class="ac-modal-chip {{ state.shareDays === d ? 'on' : '' }}"
                                            t-on-click="setShareDays(d)">
<t t-esc="plazoLabel(d)"/>
                                    </button>
                                </div>
                            </div>
                            <div class="ac-modal-acts">
                                <button class="ac-btn ghost" t-on-click="closeShare"
                                        t-att-disabled="state.shareBusy">Cancel</button>
                                <button class="ac-btn primary" t-on-click="doShare"
                                        t-att-disabled="state.shareBusy">
                                    <i t-if="state.shareBusy" class="fa fa-circle-o-notch fa-spin"/>
                                    Create link
                                </button>
                            </div>
                        </t>
                        <t t-else="">
                            <h3 class="ac-modal-title">Link ready</h3>
                            <div class="ac-modal-body">
                                <!-- La direccion se queda A LA VISTA y seleccionable:
                                     hay que copiarla, asi que un aviso que se va solo
                                     obligaria a ir a buscarla justo despues de
                                     crearla. -->
                                <div class="ac-modal-url">
                                    <input type="text" readonly="readonly" t-att-value="state.shareUrl"/>
                                </div>
                                <!-- El vencimiento que VUELVE del backend, no el que
                                     se pidio: el plan puede recortarlo en silencio. -->
                                <p t-if="state.shareExpires" class="ac-modal-hint">
                                    Expires: <t t-esc="caducidadFmt"/>
                                </p>
                            </div>
                            <div class="ac-modal-acts">
                                <button class="ac-btn ghost" t-on-click="copyShare">
                                    <i class="fa fa-clone"/> Copy
                                </button>
                                <button class="ac-btn primary" t-on-click="closeShare">Done</button>
                            </div>
                        </t>
                    </div>
                </div>
            </t>

            <!-- ==========================================================
                 Los registros detras de la cifra. El backend RE-EJECUTA la
                 consulta con los permisos del usuario, asi que puede
                 declinar con un motivo legible: eso no es un error, es la
                 explicacion, y se ensena tal cual.
                 Ventaja de estar DENTRO de Odoo: la fila abre el registro
                 de verdad, en vez de sacar al usuario a otra aplicacion.
                 ========================================================== -->
            <t t-if="state.recordsFor">
                <div class="ac-modal-wrap">
                    <div class="ac-modal-scrim" t-on-click="closeRecords"/>
                    <div class="ac-modal tall" role="dialog" aria-modal="true">
                        <span class="ac-modal-badge"><i class="fa fa-table"/></span>
                        <h3 class="ac-modal-title">
                            <t t-if="state.records and state.records.label" t-esc="state.records.label"/>
                            <t t-else="">Records</t>
                        </h3>
                        <div class="ac-modal-body">
                            <div t-if="state.recordsBusy" class="ac-loading">
                                <i class="fa fa-circle-o-notch fa-spin"/> <span>Loading…</span>
                            </div>
                            <t t-elif="state.records">
                                <p t-if="state.records.unavailable" class="ac-modal-empty"
                                   t-esc="state.records.unavailable"/>
                                <p t-elif="!(state.records.rows or []).length" class="ac-modal-empty">
                                    No records to show for this answer.
                                </p>
                                <t t-else="">
                                    <p class="ac-modal-count" t-if="state.records.total"
                                       t-esc="conteoRegistros"/>
                                    <ul class="ac-recs">
                                        <t t-foreach="state.records.rows or []" t-as="f" t-key="f.id">
                                            <li class="ac-rec" t-on-click="openRecord(f)">
                                                <span class="ac-rec-t" t-esc="f.title"/>
                                                <span t-if="f.subtitle" class="ac-rec-s" t-esc="f.subtitle"/>
                                            </li>
                                        </t>
                                    </ul>
                                </t>
                            </t>
                        </div>
                        <div class="ac-modal-acts">
                            <button class="ac-btn ghost" t-on-click="closeRecords">Close</button>
                        </div>
                    </div>
                </div>
            </t>

            <!-- ==========================================================
                 Enviar por correo. Sale con el reply-to del usuario: quien
                 lo recibe ve de quien viene y le contesta a EL.
                 ========================================================== -->
            <t t-if="state.emailFor">
                <div class="ac-modal-wrap">
                    <div class="ac-modal-scrim" t-on-click="closeEmail"/>
                    <div class="ac-modal" role="dialog" aria-modal="true">
                        <span class="ac-modal-badge"><i class="fa fa-envelope-o"/></span>
                        <h3 class="ac-modal-title">Send by email</h3>
                        <p class="ac-modal-sub">It goes out with your address as reply-to, so whoever receives it can answer you.</p>
                        <div class="ac-modal-body">
                            <span class="ac-modal-label">To (separate with commas)</span>
                            <input class="ac-modal-field" type="text" t-att-value="state.emailTo"
                                   placeholder="name@company.com" t-on-input="onEmailInput"/>
                            <button t-attf-class="ac-modal-check {{ state.emailAttach ? 'on' : '' }}"
                                    t-on-click="toggleEmailAttach">
                                <i t-attf-class="fa {{ state.emailAttach ? 'fa-check-square-o' : 'fa-square-o' }}"/>
                                Attach the spreadsheet
                            </button>
                        </div>
                        <div class="ac-modal-acts">
                            <button class="ac-btn ghost" t-on-click="closeEmail"
                                    t-att-disabled="state.emailBusy">Cancel</button>
                            <button class="ac-btn primary" t-on-click="doEmail"
                                    t-att-disabled="state.emailBusy">
                                <i t-if="state.emailBusy" class="fa fa-circle-o-notch fa-spin"/>
                                Send
                            </button>
                        </div>
                    </div>
                </div>
            </t>

            <!-- loading-state -->
            <div t-if="state.loading" class="ac-loading">
                <i class="fa fa-circle-o-notch fa-spin"/> <span>Loading…</span>
            </div>

            <!-- error-state: el estado NO se pudo leer (backend caido, sin red,
                 RPC roto). Va ANTES que "no conectado" a proposito: son cosas
                 distintas y antes se pintaban igual, asi que a quien ya tenia su
                 cuenta conectada se le pedia conectarla otra vez. -->
            <div t-elif="state.loadError" class="ac-empty">
                <span class="ac-mark ac-mark--lg"><t t-call="aski_connector.AskiMark"/></span>
                <div class="ac-empty-title">Aski isn't responding right now</div>
                <div class="ac-empty-sub" t-esc="state.loadError"/>
                <button class="ac-btn-primary" t-on-click="retryLoad">
                    <i class="fa fa-refresh"/> Try again
                </button>
            </div>

            <!-- empty-state: usuario fuera del grupo "Use the Aski chat" -->
            <div t-elif="!state.allowed" class="ac-empty">
                <span class="ac-mark ac-mark--lg"><t t-call="aski_connector.AskiMark"/></span>
                <div class="ac-empty-title">Aski chat isn't enabled for your user</div>
                <div class="ac-empty-sub">Ask an administrator to add you to the "Use the Aski chat" group to start asking your Odoo in plain language.</div>
            </div>

            <!-- empty-state: cuenta Aski no conectada -->
            <div t-elif="!state.connected" class="ac-empty">
                <span class="ac-mark ac-mark--lg"><t t-call="aski_connector.AskiMark"/></span>
                <t t-if="state.canConnect">
                    <div class="ac-empty-title">Connect Aski to start chatting</div>
                    <div class="ac-empty-sub">
                        Ask your Odoo in plain language, right here — sales, receivables,
                        top products and more.
                    </div>
                    <button class="ac-btn-primary" t-on-click="openConnect">
                        Connect my Aski account
                    </button>
                    <!-- Pedir asiento. Hasta ahora la unica salida de esta
                         pantalla era crear una cuenta propia, y el precio de un
                         plan entero aparecia al final del camino. Si su empresa
                         ya usa Aski, lo que necesita es que la sienten en esa
                         cuenta; y si no la usa nadie, el backend lo dice claro. -->
                    <button class="ac-btn-ghost" t-on-click="pedirAsiento"
                            t-att-disabled="state.seatBusy">
                        <t t-if="state.seatBusy">Asking...</t>
                        <t t-else="">My company already uses Aski — ask for a seat</t>
                    </button>
                    <div t-if="state.seatAsked" class="ac-empty-sub ac-seat-done"
                         t-esc="textoAsientoPedido"/>
                </t>
                <t t-else="">
                    <div class="ac-empty-title">Aski isn't set up yet</div>
                    <div class="ac-empty-sub">
                        Ask an administrator to connect Aski for your team.
                    </div>
                </t>
            </div>

            <t t-else="">
                <div class="ac-messages" t-ref="messages">
                    <!-- welcome hero (nueva conversacion, sin mensajes) -->
                    <div t-if="!state.messages.length" class="ac-welcome">
                        <span class="ac-mark ac-mark--lg"><t t-call="aski_connector.AskiMark"/></span>
                        <div class="ac-welcome-title">Hi, I'm Aski</div>
                        <div class="ac-welcome-sub">Ask about your business in plain language — sales, receivables, top customers and more. Read-only by default.</div>

                        <!-- Las cifras REALES de ESTA conexion, antes de la
                             primera pregunta. Llegan del backend ya formateadas y
                             ya traducidas: aqui no se calcula ni se reformatea
                             nada. -->
                        <!-- ⛔ La tarjeta se pinta solo si tiene ALGO que decir:
                             cifras, un fallo que declarar, la espera, o el aviso
                             honesto de que esta conexion aun no tiene movimientos.
                             Colgarla de `state.sugs` a secas dejaba un recuadro con
                             titulo y nada debajo en una instancia recien creada. -->
                        <div t-if="startMetrics.length or state.sugsCode or state.sugsLoading or (state.sugs and state.sugs.empty)"
                             class="ac-start">
                            <div class="ac-start-head">
                                <span class="ac-start-title" t-esc="txt.startTitle"/>
                                <span t-if="startAge" class="ac-start-age" t-esc="startAge"/>
                                <button class="ac-start-refresh"
                                        t-att-disabled="state.sugsLoading"
                                        t-on-click="loadSuggestions(true)">
                                    <i t-attf-class="fa {{ state.sugsLoading ? 'fa-circle-o-notch fa-spin' : 'fa-refresh' }}"/>
                                    <t t-esc="txt.startRefresh"/>
                                </button>
                            </div>
                            <!-- Esqueleto y no un spinner suelto: la tarjeta ya
                                 ocupa su sitio y no da el salto de maqueta cuando
                                 llegan las cifras. -->
                            <div t-if="state.sugsLoading and !state.sugs" class="ac-start-skel">
                                <div class="ac-start-skel-row"/>
                                <div class="ac-start-skel-row"/>
                            </div>
                            <div t-elif="startMetrics.length" class="ac-start-list">
                                <t t-foreach="startMetrics" t-as="mt" t-key="mt.id">
                                    <div class="ac-start-row">
                                        <div class="ac-start-k">
                                            <span t-esc="mt.label"/>
                                            <span t-if="mt.companies &gt; 1" class="ac-start-co"
                                                  t-esc="metricEmpresas(mt)"/>
                                        </div>
                                        <!-- Una moneda por renglon: el motor ya
                                             decidio no sumarlas, y juntarlas aqui
                                             seria inventar un total que no existe. -->
                                        <div class="ac-start-v">
                                            <strong t-foreach="metricLineas(mt)" t-as="ln"
                                                    t-key="ln_index" t-esc="ln"/>
                                        </div>
                                    </div>
                                </t>
                            </div>
                            <div t-if="state.sugsCode" class="ac-start-note ac-start-bad"
                                 t-esc="startErrTxt"/>
                            <div t-elif="state.sugs and state.sugs.empty" class="ac-start-note"
                                 t-esc="txt.startEmpty"/>
                            <div t-elif="state.sugs and state.sugs.stale" class="ac-start-note"
                                 t-esc="txt.startStale"/>
                        </div>
                        <!-- Las sugerencias vienen del getter `samples` (JS, con _t()):
                             la etiqueta y el texto que se ENVIA son el MISMO string
                             traducido. Antes el t-on-click llevaba el literal ingles
                             hardcodeado -> se veia en espanol pero se mandaba en ingles. -->
                        <div class="ac-samples">
                            <button t-foreach="samples" t-as="s" t-key="s.key"
                                    class="ac-sample" t-on-click="useSample(s.text)">
                                <i t-attf-class="fa {{ s.icon }}"/> <t t-esc="s.text"/>
                            </button>
                        </div>
                        <button t-if="startSections.length" class="ac-start-more"
                                t-on-click="openLibrary" t-esc="txt.startMore"/>
                    </div>
                    <t t-foreach="state.messages" t-as="m" t-key="m.id">
                        <!-- `data-msg-id` no es decorativo: es como el buscador
                             encuentra la burbuja a la que saltar. -->
                        <div t-if="m.role === 'user'" t-att-data-msg-id="m.id"
                             t-attf-class="ac-bubble ac-bubble-user {{ m.id === state.jumpHit ? 'ac-jump-hit' : '' }}">
                            <span t-esc="m.text"/>
                            <!-- Esta pregunta se hizo en analisis profundo. Va en
                                 la PREGUNTA y no en la respuesta porque es ahi
                                 donde lo guarda el backend (is_agent) y donde lo
                                 pintan la app y la web: al recargar el hilo, el
                                 distintivo sigue en su sitio. -->
                            <span t-if="m.deep" class="ac-deep-chip">
                                <i class="fa fa-flask"/> Deep
                            </span>
                            <!-- La hora va en las DOS burbujas, como en la app y
                                 en la web. -->
                            <div class="ac-meta">
                                <span class="ac-time" t-esc="bubbleTime(m.createdAt)"/>
                            </div>
                        </div>
                        <div t-elif="m.role === 'assistant'" t-att-data-msg-id="m.id"
                             t-attf-class="ac-bubble ac-bubble-bot {{ m.id === state.jumpHit ? 'ac-jump-hit' : '' }}">
                            <t t-set="graf" t-value="chartDe(m)"/>
                            <button t-if="graf" class="ac-chart-open"
                                    t-on-click="openChart(m)">
                                <i class="fa fa-bar-chart"/> View chart
                                <!-- "Convertido" NO es un detalle tecnico: dos
                                     respuestas con el mismo simbolo significan
                                     cosas distintas segun la base, y el usuario
                                     no tiene como saberlo si no se le dice. -->
                                <span t-if="graf.currency_mode === 'convertido'"
                                      class="ac-chart-conv"> · <t t-esc="etiquetaConvertido"/></span>
                            </button>
                            <div class="md" t-raw="renderMd(m.text)"/>
                            <!-- La fila se pinta SIEMPRE; lo condicional es el chip
                                 de creditos. Cuando el t-if colgaba de la fila, una
                                 respuesta sin cobro (historicos, turnos sin cargo) se
                                 quedaba sin hora, sin detalle, sin PDF y sin pulgares.
                                 La web ya lo tenia asi. -->
                            <div class="ac-meta">
                                <span class="ac-time" t-esc="bubbleTime(m.createdAt)"/>
                                <!-- Que esta respuesta salio del analisis profundo:
                                     cuesta mas creditos y conviene que se vea. -->
                                <span t-if="m.deep" class="ac-deep-chip"><i class="fa fa-flask"/> Deep</span>
                                <span t-if="m.credits !== null" class="ac-credits-chip"><i class="fa fa-bolt"/> <t t-esc="m.credits"/> credits</span>
                                <!-- Pulgares PULSABLES aqui mismo. Antes el control
                                     vivia dentro de "Details": nadie abre detalles
                                     cuando una respuesta le sirve, y menos cuando NO
                                     le sirve — reformula y sigue. Medido en produccion:
                                     2 dislikes y 0 likes en 10.389 turnos, con quejas
                                     escritas en el chat que nunca llegaron al panel.
                                     Sin backendId no hay a quien mandarle el voto (la
                                     burbuja recien enviada aun no se ha reconciliado). -->
                                <button t-if="m.backendId" title="Helpful answer"
                                        t-attf-class="ac-fb-quick {{ m.feedback === 'like' ? 'on' : '' }}"
                                        t-att-aria-pressed="m.feedback === 'like' ? 'true' : 'false'"
                                        t-on-click="setFeedback(m, 'like')">
                                    <i class="fa fa-thumbs-o-up"/>
                                </button>
                                <button t-if="m.backendId" title="Unhelpful answer"
                                        t-attf-class="ac-fb-quick {{ m.feedback === 'dislike' ? 'on' : '' }}"
                                        t-att-aria-pressed="m.feedback === 'dislike' ? 'true' : 'false'"
                                        t-on-click="setFeedback(m, 'dislike')">
                                    <i class="fa fa-thumbs-o-down"/>
                                </button>
                                <button t-if="m.backendId" class="ac-detail-btn" title="Details"
                                        t-on-click="toggleDetail(m)">
                                    <i class="fa fa-info-circle"/>
                                </button>
                            </div>
                        </div>
                        <div t-else="" class="ac-bubble ac-bubble-error">
                            <!-- Mismo renderizado que la burbuja normal: los
                                 mensajes de error del backend TAMBIEN vienen en
                                 markdown, y con t-esc el usuario leia los
                                 asteriscos crudos ("**Tu ERP no respondio.**") y
                                 perdia los saltos de parrafo. -->
                            <!-- El icono va en su PROPIO nodo y el markdown en un
                                 div hermano: mdToHtml emite bloques (<p>, <ul>)
                                 y meterlos en un <span> es HTML invalido, el
                                 navegador los reubica y rompe la burbuja. -->
                            <div class="ac-error-body">
                                <i class="fa fa-exclamation-triangle"/>
                                <div class="md" t-raw="renderMd(m.text)"/>
                            </div>
                            <div class="ac-error-actions">
                                <!-- Se le pasa el MENSAJE, no solo el texto: al
                                     reintentar hay que retirar esta burbuja, o
                                     los errores se van apilando en el hilo. -->
                                <button class="ac-btn-link" t-on-click="retry(m)">
                                    Retry
                                </button>
                                <button t-if="m.sinCreditos and !state.partnerManaged"
                                        class="ac-btn-link" t-on-click="openBilling">
                                    Top up credits
                                </button>
                            </div>
                        </div>
                    </t>
                    <!-- "Aski esta pensando" -->
                    <div t-if="state.sending" class="ac-bubble ac-bubble-bot ac-thinking">
                        <span class="ac-typing"><i/><i/><i/></span>
                        <!-- El tiempo REAL transcurrido (no fases inventadas: el
                             backend no reporta progreso por esta via). Aparece a
                             los 3 s para no parpadear en las respuestas rapidas;
                             en analisis profundo se avisa de que va para largo,
                             que puede pasar del minuto. -->
                        <span t-if="state.elapsed >= 3" class="ac-thinking-note">
                            <t t-if="state.sendingDeep">Digging deeper — this one takes a while.</t>
                            <t t-else="">Working on it…</t>
                            <span class="ac-thinking-secs"><t t-esc="state.elapsed"/>s</span>
                        </span>
                    </div>
                </div>

                <div class="ac-composer">
                    <!-- ACCIONES pendientes de confirmar. Van pegadas al
                         compositor, no en una hoja aparte: son lo unico del chat
                         que ESCRIBE en el ERP, y algo asi no puede estar a dos
                         clics de distancia ni descubrirse por accidente. -->
                    <div t-if="state.actions.length" class="ac-actions">
                        <div t-foreach="state.actions" t-as="a" t-key="a.id"
                             class="ac-action">
                            <div class="ac-action-txt">
                                <i class="fa fa-bolt"/>
                                <span t-esc="a.summary || a.kind"/>
                            </div>
                            <div t-if="state.confirmActionId === a.id" class="ac-action-confirm">
                                <span>This writes to your Odoo. Go ahead?</span>
                                <button class="ac-btn-danger" data-act="confirm"
                                        t-att-disabled="state.actionBusy"
                                        t-on-click="confirmarAccion(a)">Yes, do it</button>
                                <button class="ac-btn-ghost-sm"
                                        t-on-click="cancelarConfirmacion">Not now</button>
                            </div>
                            <div t-else="" class="ac-action-btns">
                                <button class="ac-btn-primary-sm" data-act="review"
                                        t-on-click="pedirConfirmacion(a)">Review</button>
                                <button class="ac-btn-ghost-sm"
                                        t-on-click="descartarAccion(a)">Dismiss</button>
                            </div>
                        </div>
                    </div>
                    <!-- REGISTRO ABIERTO: sobre que ficha se esta preguntando.
                         Va PEGADO al compositor y no en la cabecera a proposito:
                         es lo que el usuario tiene que ver mientras escribe. Un
                         ambito invisible convierte "¿cuanto debe?" en una cifra
                         que no cuadra con la pantalla, sin que nadie sepa por que.
                         Siempre se puede quitar con la ×. -->
                    <div t-if="state.record" class="ac-scope" role="status">
                        <i class="fa fa-thumb-tack ac-scope-pin" aria-hidden="true"/>
                        <span class="ac-scope-lbl">Asking about</span>
                        <span class="ac-scope-name" t-att-title="state.record.label">
                            <t t-esc="state.record.label or (state.record.model + ' #' + state.record.resId)"/>
                        </span>
                        <button class="ac-scope-x" t-on-click="clearScope"
                                title="Ask about everything again, not just this record"
                                aria-label="Remove record scope">
                            <i class="fa fa-times" aria-hidden="true"/>
                        </button>
                    </div>

                    <!-- El agente avisa de que la consulta es pesada y espera el
                         visto bueno, igual que en la app. -->
                    <div t-if="state.pendingHeavy" class="ac-heavy">
                        <div class="ac-heavy-txt">This one needs a heavier analysis. Run it anyway?</div>
                        <div class="ac-heavy-actions">
                            <button class="ac-confirm-cancel" t-on-click="cancelHeavy">Not now</button>
                            <button class="ac-confirm-ok" t-on-click="confirmHeavy"
                                    t-att-disabled="state.sending">Run it</button>
                        </div>
                    </div>

                    <!-- Analisis profundo: solo se ofrece si el plan lo incluye
                         (agentEnabled lo dice el backend). Ofrecerlo siempre era
                         prometer algo que acaba en un 403. -->
                    <div t-if="state.agentEnabled or state.conversationId" class="ac-composer-tools">
                        <button t-if="state.conversationId" class="ac-fresh"
                                t-on-click="clearContext" t-att-disabled="state.clearing || state.sending"
                                title="Forget what we talked about, without deleting the thread">
                            <i t-attf-class="fa {{ state.clearing ? 'fa-circle-o-notch fa-spin' : 'fa-refresh' }}"/>
                            Start fresh
                        </button>
                        <span t-else="" class="ac-tools-gap"/>
                        <button t-if="state.agentEnabled"
                                t-attf-class="ac-deep-toggle {{ state.deepMode ? 'on' : '' }}"
                                t-on-click="toggleDeepMode" t-att-disabled="state.sending"
                                title="Deep analysis: slower, but digs through more data">
                            <i class="fa fa-flask"/> Deep analysis
                        </button>
                    </div>

                    <div class="ac-composer-inner">
                        <button t-if="state.conversationId" class="ac-export" title="Export last answer to PDF"
                                t-on-click="exportPdf" t-att-disabled="state.exporting">
                            <i t-attf-class="fa {{ state.exporting ? 'fa-circle-o-notch fa-spin' : 'fa-file-pdf-o' }}"/>
                        </button>
                        <!-- textarea y NO input: el handler distingue Shift+Enter
                             para partir la linea, y en un <input> eso no puede
                             pasar. Sin `value` atado al estado — ver composerRef.
                             Tampoco se DESHABILITA mientras Aski responde: se
                             puede ir escribiendo la siguiente pregunta, solo el
                             boton de enviar espera. -->
                        <textarea t-ref="composer" rows="1" t-att-maxlength="MAX_PROMPT"
                                  placeholder="Ask Aski anything about your Odoo…"
                                  t-on-input="onInput" t-on-keydown="onKeydown"/>
                        <button class="ac-send" t-on-click="send()"
                                t-att-disabled="state.sending || !state.input.trim()">
                            <i t-attf-class="fa {{ state.sending ? 'fa-circle-o-notch fa-spin' : 'fa-paper-plane' }}"/>
                        </button>
                    </div>
                </div>
            </t>

            </t>

            <!-- Detalle de UNA respuesta. Va ENCIMA del chat (scrim + hoja), como
                 la hoja de Android y la de la web. Colgado bajo la burbuja se abria
                 DEBAJO del contenido: con una respuesta larga (una tabla) no se veia
                 y habia que adivinar que algo se habia desplegado.
                 Se monta al final de .aski-chat, que ya es `position: relative` con
                 `overflow: hidden`, asi queda contenido igual en pantalla completa
                 que en la burbuja estrecha del systray. -->
            <!-- AVISOS PROGRAMADOS. Misma hoja que el detalle de una respuesta:
                 el widget ya tiene ese patron y una ventana nueva con otro
                 aspecto se leeria como otra aplicacion. -->
            <div t-if="state.insightsOpen" class="ac-sheet-wrap">
                <div class="ac-scrim" t-on-click="closeInsights"/>
                <div class="ac-sheet" role="dialog" aria-modal="true">
                    <div class="ac-sheet-grip"/>
                    <div class="ac-sheet-head">
                        <span class="ac-sheet-title">Scheduled alerts</span>
                        <button class="ac-sheet-close" title="Close" t-on-click="closeInsights">
                            <i class="fa fa-times"/>
                        </button>
                    </div>

                    <!-- Los dos avisos FIJOS: no se crean, se encienden. No
                         gastan cupo, asi que van arriba y sin ceremonia. Las
                         etiquetas son las mismas que en la app y la web. -->
                    <div t-if="!state.newKind and !state.insightNewFor and !state.connSelFor"
                         class="ac-ins-fijos">
                        <div class="ac-ins-fijo">
                            <div class="ac-ins-fijo-t">
                                <b>Daily digest</b>
                                <small>Your key figures, first thing in the morning.</small>
                                <!-- A QUE conexiones cubre. Es UN aviso por
                                     cuenta que puede hablar de varias empresas en
                                     el mismo mensaje: sin decirlo, quien tiene dos
                                     Odoo cree que este resumen es solo de aqui. -->
                                <t t-call="aski_connector.AskiInsightConns">
                                    <t t-set="fijo" t-value="resumenDiario"/>
                                </t>
                            </div>
                            <button class="ac-btn-ghost-sm" data-act="digest"
                                    t-att-disabled="state.insightBusy"
                                    t-on-click="alternarResumen">
                                <t t-if="resumenDiario and resumenDiario.enabled">Turn off</t>
                                <t t-else="">Turn on</t>
                            </button>
                        </div>
                        <div class="ac-ins-fijo">
                            <div class="ac-ins-fijo-t">
                                <b>Day closing</b>
                                <small>What actually moved today, in the evening.</small>
                                <t t-call="aski_connector.AskiInsightConns">
                                    <t t-set="fijo" t-value="cierreDelDia"/>
                                </t>
                            </div>
                            <button class="ac-btn-ghost-sm" data-act="closing"
                                    t-att-disabled="state.insightBusy"
                                    t-on-click="alternarCierre">
                                <t t-if="cierreDelDia and cierreDelDia.enabled">Turn off</t>
                                <t t-else="">Turn on</t>
                            </button>
                        </div>
                    </div>

                    <!-- ELEGIR CONEXIONES. Sustituye al contenido de la hoja
                         mientras esta abierto, igual que hacen el alta de vigia y
                         la de recordatorio: dos capas apiladas dentro de la misma
                         hoja se leen como que el boton no hizo nada. -->
                    <div t-if="state.connSelFor" class="ac-ins-new">
                        <div class="ac-ins-q" t-esc="txt.connTitle"/>
                        <div class="ac-ins-note" t-esc="txt.connHelp"/>
                        <div class="ac-conn-list">
                            <label t-foreach="state.conexiones" t-as="c" t-key="c.id"
                                   class="ac-conn-row">
                                <input type="checkbox"
                                       t-att-checked="conexionMarcada(c.id)"
                                       t-on-change="alternarConexion(c.id)"/>
                                <span t-esc="nombreConexion(c)"/>
                            </label>
                        </div>
                        <div class="ac-ins-note" t-esc="txt.connLast"/>
                        <div class="ac-ins-btns2">
                            <button class="ac-btn-primary-sm" t-on-click="guardarConexiones"
                                    t-att-disabled="state.connSaving">
                                <t t-if="state.connSaving"><t t-esc="txt.connSaving"/></t>
                                <t t-else=""><t t-esc="txt.connSave"/></t>
                            </button>
                            <button class="ac-btn-ghost-sm" t-on-click="cerrarConexiones"
                                    t-esc="txt.connCancel"/>
                        </div>
                    </div>

                    <!-- Y los dos que si se configuran. -->
                    <div t-if="!state.newKind and !state.insightNewFor and !state.connSelFor" class="ac-ins-nuevos">
                        <button class="ac-btn-ghost-sm" data-act="new-watch"
                                t-on-click="abrirVigia">
                            <i class="fa fa-eye"/> Watch a figure
                        </button>
                        <button class="ac-btn-ghost-sm" data-act="new-reminder"
                                t-on-click="abrirRecordatorio">
                            <i class="fa fa-clock-o"/> Add a reminder
                        </button>
                    </div>

                    <!-- Que hay disponible. Sin esto la hoja ofrecia "vigilar
                         una cifra" sin decir cuales, y solo lo descubria quien
                         ya lo sabia. -->
                    <div t-if="!state.newKind and !state.insightNewFor and !state.connSelFor" class="ac-cat-link">
                        <button data-act="cat-watch"
                                t-on-click="abrirCatalogo('watch')">
                            <i class="fa fa-list-ul"/> See everything I can watch and remind you about
                        </button>
                    </div>

                    <!-- VIGIA -->
                    <div t-if="state.newKind === 'watch'" class="ac-ins-new">
                        <div class="ac-ins-q">New watch</div>
                        <div t-if="state.catalogoCargando" class="ac-ins-note">Reading your ERP…</div>
                        <div t-elif="!state.metricas.length" class="ac-ins-note">
                            We found no figure to watch in this connection.
                        </div>
                        <t t-else="">
                            <div class="ac-ins-row">
                                <label>What to watch</label>
                                <select t-model="state.watchMetric">
                                    <option t-foreach="state.metricas" t-as="m" t-key="m.id"
                                            t-att-value="m.id"><t t-esc="m.label"/></option>
                                </select>
                            </div>
                            <!-- El valor de HOY, para que el limite que se teclea
                                 tenga contra que compararse. -->
                            <div t-if="metricaElegida" class="ac-ins-note">
                                <t t-esc="textoHoy"/>
                            </div>
                            <div class="ac-ins-row">
                                <label>When</label>
                                <select t-model="state.watchOp">
                                    <option value="&gt;">Goes above</option>
                                    <option value="&lt;">Falls below</option>
                                    <option value="%&gt;">Rises more than</option>
                                    <option value="%&lt;">Falls more than</option>
                                </select>
                            </div>
                            <div class="ac-ins-row">
                                <label>Limit</label>
                                <div class="ac-ins-field">
                                    <input type="text" t-model="state.watchValue"
                                           t-att-placeholder="esVariacion ? '%' : ''"/>
                                    <span t-if="sufijoLimite" class="ac-ins-suf"
                                          t-esc="sufijoLimite"/>
                                </div>
                            </div>
                            <!-- ⛔ Cuando la cifra llega en varias monedas, el umbral
                                 se compara contra UNA. Sin decirlo, alguien teclea
                                 pensando en soles un limite que se mide en dolares y
                                 el aviso salta cuando no debe (o calla cuando si). -->
                            <div t-if="avisoMoneda" class="ac-ins-note" t-esc="avisoMoneda"/>
                            <div t-if="esVariacion" class="ac-ins-note">
                                The first run only takes the baseline.
                            </div>
                            <div class="ac-ins-note">
                                It lands in your Odoo inbox, with your own permissions.
                            </div>
                            <div class="ac-ins-btns2">
                                <button class="ac-btn-primary-sm" t-on-click="guardarVigia"
                                        t-att-disabled="state.insightSaving">
                                    <t t-if="state.insightSaving">Saving...</t>
                                    <t t-else="">Save alert</t>
                                </button>
                                <button class="ac-btn-ghost-sm" t-on-click="cerrarAlta">Cancel</button>
                            </div>
                        </t>
                    </div>

                    <!-- RECORDATORIO -->
                    <div t-if="state.newKind === 'reminder'" class="ac-ins-new">
                        <div class="ac-ins-q">New reminder</div>
                        <div t-if="state.catalogoCargando" class="ac-ins-note">Reading your ERP…</div>
                        <div t-elif="!state.temas.length" class="ac-ins-note">
                            There is nothing to remind you about in this connection.
                        </div>
                        <t t-else="">
                            <div class="ac-ins-row">
                                <label>What to remind you about</label>
                                <select t-model="state.remTopic">
                                    <option t-foreach="state.temas" t-as="x" t-key="x.id"
                                            t-att-value="x.id"><t t-esc="x.label"/></option>
                                </select>
                            </div>
                            <div class="ac-ins-row">
                                <label>Days</label>
                                <input type="number" t-model="state.remDays" min="0" max="365"/>
                            </div>
                            <div class="ac-ins-note">
                                Aski only writes when something new shows up. It never repeats one it already named.
                            </div>
                            <div class="ac-ins-btns2">
                                <button class="ac-btn-primary-sm" t-on-click="guardarRecordatorio"
                                        t-att-disabled="state.insightSaving">
                                    <t t-if="state.insightSaving">Saving...</t>
                                    <t t-else="">Save alert</t>
                                </button>
                                <button class="ac-btn-ghost-sm" t-on-click="cerrarAlta">Cancel</button>
                            </div>
                        </t>
                    </div>


                    <!-- Alta desde una respuesta que Aski YA resolvio. -->
                    <div t-if="state.insightNewFor" class="ac-ins-new">
                        <div class="ac-ins-q" t-esc="_preguntaDe(state.insightNewFor)"/>
                        <div class="ac-ins-row">
                            <label>How often</label>
                            <select t-model="state.insightFreq">
                                <option value="daily">Every day</option>
                                <option value="weekly">Every week</option>
                            </select>
                        </div>
                        <div class="ac-ins-row">
                            <label>At</label>
                            <select t-model="state.insightHour">
                                <t t-foreach="[6,7,8,9,10,12,15,17,18]" t-as="h" t-key="h">
                                    <option t-att-value="h"><t t-esc="h"/>:00</option>
                                </t>
                            </select>
                        </div>
                        <div class="ac-ins-note">
                            It lands in your Odoo inbox, with your own permissions.
                        </div>
                        <button class="ac-btn-primary-sm" t-on-click="guardarAviso"
                                t-att-disabled="state.insightSaving">
                            <t t-if="state.insightSaving">Saving...</t>
                            <t t-else="">Save alert</t>
                        </button>
                    </div>

                    <div t-if="state.newKind or state.connSelFor" class="ac-ins-oculto"/>
                    <div t-elif="state.insightsLoading" class="ac-drawer-empty">Loading...</div>
                    <div t-elif="state.insightsErr" class="ac-drawer-empty">
                        Could not load your alerts. Try again in a moment.
                    </div>
                    <div t-elif="!avisosListados.length and !state.insightNewFor and !state.connSelFor"
                         class="ac-drawer-empty">
                        No alerts yet. Ask something, then use the bell on the answer to get it every morning.
                    </div>

                    <div t-if="!state.newKind and !state.connSelFor" t-foreach="avisosListados"
                         t-as="f" t-key="f.id" class="ac-ins-item">
                        <div class="ac-ins-main">
                            <div class="ac-ins-title" t-esc="f.title || f.kind"/>
                            <div class="ac-ins-sub">
                                <t t-esc="etiquetaFrecuencia(f)"/> ·
                                <t t-esc="f.send_hour_local"/>:00
                                <t t-if="f.paused_reason"> · <t t-esc="motivoPausa(f)"/></t>
                            </div>
                        </div>
                        <div class="ac-ins-btns">
                            <button t-if="f.paused_reason" class="ac-btn-ghost-sm" data-act="resume"
                                    t-att-disabled="state.insightBusy === f.id"
                                    t-on-click="reanudarAviso(f)">Resume</button>
                            <button t-else="" class="ac-btn-ghost-sm" data-act="toggle"
                                    t-att-disabled="state.insightBusy === f.id"
                                    t-on-click="toggleInsight(f)">
                                <t t-if="f.enabled">Pause</t>
                                <t t-else="">Turn on</t>
                            </button>
                            <button class="ac-btn-ghost-sm" data-act="remove"
                                    t-att-disabled="state.insightBusy === f.id"
                                    t-on-click="borrarAviso(f)">Remove</button>
                        </div>
                    </div>

                    <div t-if="state.insightLimit and !state.connSelFor" class="ac-ins-quota"
                         t-esc="textoCupoAvisos"/>
                </div>
            </div>

            <!-- EL CATALOGO: que se puede vigilar, que se puede recordar y
                 que se puede EJECUTAR sobre el ERP.

                 Se sirve del backend y no se escribe aqui: sale de los mismos
                 registros que ejecutan, asi que si algo se lista es porque
                 existe, y un tema nuevo aparece sin publicar version del modulo.

                 Y se ensena ENTERO, marcando lo que no va en esta conexion. Los
                 catalogos que alimentan el dialogo de crear filtran a lo que la
                 instancia sostiene -y hacen bien-, pero eso deja al usuario sin
                 saber que se pierde. Aqui se ve el mapa completo. -->
            <div t-if="state.catOpen" class="ac-sheet-wrap">
                <div class="ac-scrim" t-on-click="cerrarCatalogo"/>
                <div class="ac-sheet ac-sheet-cat" role="dialog" aria-modal="true">
                    <div class="ac-sheet-grip"/>
                    <div class="ac-sheet-head">
                        <span class="ac-sheet-title">What I can do</span>
                        <button class="ac-sheet-close" title="Close" t-on-click="cerrarCatalogo">
                            <i class="fa fa-times"/>
                        </button>
                    </div>

                    <div t-if="state.catCargando" class="ac-ins-note">Reading your ERP…</div>
                    <div t-elif="catVacio" class="ac-ins-note">
                        The catalog could not be read. Check the connection and try again.
                    </div>
                    <t t-else="">
                        <div class="ac-cat-leyenda">
                            Everything is listed. What your connection does not support is
                            marked, so you can see what you are missing.
                        </div>

                        <div t-if="gruposVigia.length" class="ac-cat-sec">
                            <div class="ac-cat-sec-h">
                                <b><i class="fa fa-eye"/> Figures I can watch</b>
                                <span t-if="cuentaAvisos" class="ac-cat-count" t-esc="cuentaAvisos"/>
                            </div>
                            <div t-foreach="gruposVigia" t-as="g" t-key="g.id" class="ac-cat-grupo">
                                <div class="ac-cat-grupo-t" t-esc="g.title"/>
                                <div t-foreach="g.items" t-as="it" t-key="it.id"
                                     class="ac-cat-item"
                                     t-att-class="{'ac-cat-no': it.available === false}">
                                    <div class="ac-cat-item-t">
                                        <b t-esc="it.name"/>
                                        <small t-esc="it.what"/>
                                        <small t-if="it.example" class="ac-cat-ej" t-esc="it.example"/>
                                    </div>
                                    <span t-if="it.available === false" class="ac-cat-no-t">
                                        Not in your connection
                                    </span>
                                </div>
                            </div>
                        </div>

                        <div t-if="gruposRecordatorio.length" class="ac-cat-sec">
                            <div class="ac-cat-sec-h">
                                <b><i class="fa fa-clock-o"/> What I can remind you about</b>
                            </div>
                            <div t-foreach="gruposRecordatorio" t-as="g" t-key="g.id" class="ac-cat-grupo">
                                <div class="ac-cat-grupo-t" t-esc="g.title"/>
                                <div t-foreach="g.items" t-as="it" t-key="it.id"
                                     class="ac-cat-item"
                                     t-att-class="{'ac-cat-no': it.available === false}">
                                    <div class="ac-cat-item-t">
                                        <b t-esc="it.name"/>
                                        <small t-esc="it.what"/>
                                        <small t-if="it.example" class="ac-cat-ej" t-esc="it.example"/>
                                    </div>
                                    <span t-if="it.available === false" class="ac-cat-no-t">
                                        Not in your connection
                                    </span>
                                </div>
                            </div>
                        </div>

                        <div t-if="gruposAccion.length" class="ac-cat-sec">
                            <div class="ac-cat-sec-h">
                                <b><i class="fa fa-magic"/> What I can do in your ERP</b>
                                <span t-if="cuentaAcciones" class="ac-cat-count" t-esc="cuentaAcciones"/>
                            </div>
                            <div class="ac-ins-note">Nothing runs on its own: I propose it and you confirm it, with your own permissions.</div>
                            <div t-foreach="gruposAccion" t-as="g" t-key="g.id" class="ac-cat-grupo">
                                <div class="ac-cat-grupo-t" t-esc="g.title"/>
                                <div t-foreach="g.items" t-as="it" t-key="it.verb"
                                     class="ac-cat-item"
                                     t-att-class="{'ac-cat-no': it.available === false}">
                                    <div class="ac-cat-item-t">
                                        <b t-esc="it.name"/>
                                        <small t-esc="it.what"/>
                                        <small t-if="it.examples and it.examples.length" class="ac-cat-ej"
                                               t-esc="it.examples[0]"/>
                                    </div>
                                    <span t-if="it.available === false" class="ac-cat-no-t">
                                        Not in your connection
                                    </span>
                                </div>
                            </div>
                        </div>
                    </t>
                </div>
            </div>

            <!-- MI EQUIPO. Mismas etiquetas que la app: quien mira esto en
                 Odoo y en el movil tiene que leer lo mismo. Es de LECTURA —
                 invitar y quitar se hacen donde ya estaban, en la app o la web—
                 porque lo que falta aqui es SABER, no gestionar. -->
            <div t-if="state.equipoOpen" class="ac-sheet-wrap">
                <div class="ac-scrim" t-on-click="cerrarEquipo"/>
                <div class="ac-sheet" role="dialog" aria-modal="true">
                    <div class="ac-sheet-grip"/>
                    <div class="ac-sheet-head">
                        <span class="ac-sheet-title">My team</span>
                        <button class="ac-sheet-close" title="Close" t-on-click="cerrarEquipo">
                            <i class="fa fa-times"/>
                        </button>
                    </div>

                    <div t-if="state.equipoCargando" class="ac-ins-note">Loading...</div>

                    <t t-else="">
                        <!-- Quien se sienta en la cuenta de OTRO se ve a si mismo:
                             de quien es la cuenta y cuanto lleva gastado. -->
                        <div t-if="esAsiento" class="ac-team-seat">
                            <div class="ac-ins-q">You are on a team</div>
                            <div class="ac-ins-note" t-esc="textoAsientoCuenta"/>
                            <div class="ac-ins-note" t-esc="textoAsientoConsumo"/>
                            <!-- Sin precios ni boton de compra: quien paga es el
                                 titular. Lo que si hace falta es poder pedirselo
                                 sin salir de aqui. -->
                            <div class="ac-ins-btns2">
                                <button class="ac-btn-ghost-sm" t-on-click="pedirMasCreditos"
                                        t-att-disabled="state.creditosPedidos">
                                    <t t-if="state.creditosPedidos">Asked</t>
                                    <t t-else="">Ask for more credits</t>
                                </button>
                            </div>
                        </div>

                        <t t-elif="capacidadEquipo and capacidadEquipo.supported">
                            <!-- La MISMA tarjeta que la app y la web: rotulo, la
                                 cifra grande, la barra por tramos y la leyenda.
                                 Un "0 de 3" suelto no dice si el hueco es libre o
                                 esta invitado, que es justo lo que se viene a ver. -->
                            <div class="ac-team-cap">
                                <div class="ac-team-cap-top">
                                    <div class="ac-team-cifra">
                                        <small class="ac-team-rotulo">Seats</small>
                                        <div>
                                            <b t-esc="capacidadEquipo.used"/>
                                            <small t-esc="textoDeTotal"/>
                                        </div>
                                    </div>
                                    <div t-if="state.equipo.pool_total" class="ac-team-bolsa">
                                        <small>Team pot</small>
                                        <b t-esc="bolsaFmt"/>
                                        <small t-esc="textoCuotaPlan"/>
                                    </div>
                                </div>
                                <div class="ac-team-barra">
                                    <i class="b-used" t-attf-style="width: {{ pctAsientos.usados }}%"/>
                                    <i class="b-inv" t-attf-style="width: {{ pctAsientos.invitados }}%"/>
                                </div>
                                <div class="ac-team-leyenda">
                                    <span><i class="ac-team-dot used"/><t t-esc="leyendaAsientos.usados"/></span>
                                    <span t-if="capacidadEquipo.invited"><i class="ac-team-dot inv"/><t t-esc="leyendaAsientos.invitados"/></span>
                                    <span><i class="ac-team-dot"/><t t-esc="leyendaAsientos.libres"/></span>
                                </div>
                            </div>

                            <!-- ALTA. Quien usa Aski dentro de Odoo suele estar
                                 ahi por no querer llevar sus datos a otra
                                 pantalla: mandarle a la web a dar de alta a un
                                 companero es mandarle justo a lo que evita. -->
                            <div t-if="state.invAbierto" class="ac-ins-new">
                                <div class="ac-ins-q">Invite someone</div>
                                <div class="ac-ins-campo">
                                    <label>Email</label>
                                    <div class="ac-ins-field">
                                        <input type="text" t-model="state.invEmail"
                                               t-att-placeholder="txt.invitePh"/>
                                    </div>
                                </div>
                                <!-- ⛔ Dos botones, no un desplegable nativo: son
                                     SOLO dos opciones y las dos hay que poder
                                     leerlas de un vistazo. Un `select` las
                                     esconde detras de un clic y encima cada
                                     navegador lo pinta a su manera. Mismo control
                                     que en la app. -->
                                <div class="ac-ins-campo">
                                    <label>What they can do</label>
                                    <div class="ac-chips">
                                        <button t-attf-class="ac-chip {{ state.invRol === 'member' ? 'on' : '' }}"
                                                t-on-click="fijarEstado('invRol', 'member')">Member</button>
                                        <button t-attf-class="ac-chip {{ state.invRol === 'admin' ? 'on' : '' }}"
                                                t-on-click="fijarEstado('invRol', 'admin')">Administrator</button>
                                    </div>
                                </div>
                                <div class="ac-ins-note">
                                    A member asks, creates their own alerts and shares answers. An administrator also invites people and sees everyone's usage.
                                </div>
                                <div class="ac-ins-campo">
                                    <label>Monthly cap</label>
                                    <div class="ac-ins-field">
                                        <input type="number" t-model="state.invTope" min="0"/>
                                        <span class="ac-ins-suf">credits</span>
                                    </div>
                                </div>
                                <div class="ac-ins-note">
                                    Optional. With no cap, one person can spend the whole team pot.
                                </div>
                                <div t-if="costoAsiento" class="ac-ins-note ac-team-costo"
                                     t-esc="costoAsiento"/>
                                <div class="ac-ins-btns2">
                                    <button class="ac-btn-primary-sm" t-on-click="enviarInvitacion"
                                            t-att-disabled="state.invBusy">
                                        <t t-if="state.invBusy">Sending...</t>
                                        <t t-else="">Send invitation</t>
                                    </button>
                                    <button class="ac-btn-ghost-sm" t-on-click="cerrarInvitar">Cancel</button>
                                </div>
                            </div>

                            <t t-else="">
                            <div class="ac-ins-q">People</div>
                            <div t-foreach="personasEquipo" t-as="p" t-key="p.id" class="ac-ins-item">
                                <div class="ac-ins-main">
                                    <div class="ac-ins-title" t-esc="p.email"/>
                                    <div class="ac-ins-sub" t-esc="subDePersona(p)"/>
                                </div>
                                <!-- Las acciones, aqui mismo. Tres estados, como
                                     en la app: una invitacion sin aceptar se
                                     anula, un asiento se quita o se devuelve. -->
                                <div t-if="!p.is_owner" class="ac-ins-btns">
                                    <button t-if="p.status === 'invited'" class="ac-btn-ghost-sm"
                                            t-att-disabled="state.seatBusyId === p.id"
                                            t-on-click="anularInvitacion(p)">Cancel invitation</button>
                                    <button t-elif="p.status === 'suspended'" class="ac-btn-ghost-sm"
                                            t-att-disabled="state.seatBusyId === p.id"
                                            t-on-click="devolverAsiento(p)">Give the seat back</button>
                                    <button t-else="" class="ac-btn-ghost-sm"
                                            t-att-disabled="state.seatBusyId === p.id"
                                            t-on-click="pedirQuitar(p)">Take the seat back</button>
                                    <!-- Cambiar rol y tope sin salir de aqui. El
                                         backend lo admite; hasta ahora subirle el
                                         tope a alguien obligaba a irse a la web. -->
                                    <button class="ac-btn-ghost-sm"
                                            t-att-disabled="state.seatBusyId === p.id"
                                            t-on-click="abrirEditarAsiento(p)">Change limits</button>
                                </div>
                            </div>

                            <!-- Editar un asiento. Abre bajo la persona, no en
                                 otra pantalla: se esta mirando su consumo y el
                                 tope se decide con esa cifra delante. -->
                            <div t-if="state.seatEdit and state.seatEdit.id === p.id" class="ac-ins-new">
                                <div class="ac-ins-q">Change limits</div>
                                <div class="ac-ins-campo">
                                    <label>What they can do</label>
                                    <div class="ac-chips">
                                        <button t-attf-class="ac-chip {{ state.seatEditRol === 'member' ? 'on' : '' }}"
                                                t-on-click="fijarEstado('seatEditRol', 'member')">Member</button>
                                        <button t-attf-class="ac-chip {{ state.seatEditRol === 'admin' ? 'on' : '' }}"
                                                t-on-click="fijarEstado('seatEditRol', 'admin')">Administrator</button>
                                    </div>
                                </div>
                                <div class="ac-ins-campo">
                                    <label>Monthly cap</label>
                                    <div class="ac-ins-field">
                                        <input type="number" t-model="state.seatEditTope" min="0"/>
                                        <span class="ac-ins-suf">credits</span>
                                    </div>
                                </div>
                                <div class="ac-ins-note">
                                    Leave it empty for no cap. With no cap, one person can spend the whole team pot.
                                </div>
                                <div class="ac-ins-btns2">
                                    <button class="ac-btn-primary-sm"
                                            t-att-disabled="state.seatBusyId === p.id"
                                            t-on-click="guardarAsiento">Save</button>
                                    <button class="ac-btn-ghost-sm"
                                            t-on-click="cerrarEditarAsiento">Not now</button>
                                </div>
                            </div>

                            <!-- Quitar CORTA el acceso de una persona: se pregunta
                                 antes, aqui dentro, nunca con un confirm del
                                 navegador. -->
                            <div t-if="state.seatPorQuitar" class="ac-action-confirm">
                                <div t-esc="textoQuitarAsiento"/>
                                <div class="ac-ins-btns2">
                                    <button class="ac-btn-primary-sm" t-on-click="confirmarQuitar"
                                            t-att-disabled="state.seatBusyId">Take the seat back</button>
                                    <button class="ac-btn-ghost-sm" t-on-click="cancelarQuitar">Not now</button>
                                </div>
                            </div>

                            <div t-if="state.invEnlace" class="ac-ins-new">
                                <div class="ac-ins-q">Pass them this link</div>
                                <div class="ac-ins-note" t-esc="state.invEnlace"/>
                                <div class="ac-ins-btns2">
                                    <button class="ac-btn-ghost-sm" t-on-click="copiarEnlaceInvitacion">Copy link</button>
                                </div>
                            </div>

                            <div class="ac-ins-nuevos">
                                <button class="ac-btn-ghost-sm" data-act="invite"
                                        t-on-click="abrirInvitar">
                                    <i class="fa fa-user-plus"/> Invite someone
                                </button>
                                <!-- ⛔ Cliente de un socio: aqui NO se compra ni
                                     se manda a "contactar a ventas". La tarifa la
                                     pone el socio, y quien usa Aski dentro de
                                     Odoo esta aqui por no salir a otra pantalla.
                                     Se deja una peticion REGISTRADA que el socio
                                     ve en su panel y resuelve de un toque. -->
                                <!-- Con socio se PIDE; sin socio se COMPRA.
                                     ⛔ El comentario va aqui arriba y no entre el
                                     t-if y el t-else: en medio rompe el
                                     emparejamiento de QWeb.
                                     Y comprar SI se puede enlazar desde aqui: la
                                     regla que lo prohibe es de Google Play y
                                     afecta a la app de Android, no a una pagina
                                     dentro de Odoo. Copiar alli el "contactar a
                                     ventas" habria sido arrastrar una limitacion
                                     ajena y dejar a esta gente sin salida. -->
                                <button t-if="esDeSocio" class="ac-btn-ghost-sm"
                                        data-act="ask-seat"
                                        t-att-disabled="state.pidiendo or yaPedido('seat', '')"
                                        t-on-click="pedirAlProveedor('seat', null)">
                                    <t t-if="yaPedido('seat', '')">
                                        <i class="fa fa-check"/> Seats requested
                                    </t>
                                    <t t-else="">
                                        <i class="fa fa-user-plus"/> Ask for more seats
                                    </t>
                                </button>
                                <button t-else="" class="ac-btn-ghost-sm"
                                        data-act="buy-seat"
                                        t-on-click="comprarAsientos">
                                    <i class="fa fa-external-link"/> Add seats on the web
                                </button>
                            </div>

                            <!-- Lo demas que solo puede darle su proveedor. Va
                                 aqui y no en otra pantalla porque es el mismo
                                 gesto: pedirle algo a quien lleva tu cuenta. -->
                            <div t-if="esDeSocio" class="ac-ins-nuevos">
                                <button class="ac-btn-ghost-sm" data-act="ask-plan"
                                        t-on-click="abrirPedir('plan')">
                                    <i class="fa fa-arrow-up"/> Ask for a plan change
                                </button>
                                <button class="ac-btn-ghost-sm" data-act="ask-topup"
                                        t-on-click="abrirPedir('topup')">
                                    <i class="fa fa-bolt"/> Ask for a top-up
                                </button>
                            </div>

                            <!-- El cliente elige el producto CONCRETO: pedir "mas"
                                 a secas obliga al socio a escribirle antes de
                                 poder resolverlo, y entonces no se ahorra la
                                 conversacion que esto viene a ahorrar. -->
                            <div t-if="state.pedirAbierto === 'plan'" class="ac-ins-new">
                                <div class="ac-ins-q">Which plan do you want?</div>
                                <div t-if="!state.catalogo" class="ac-ins-note">Reading the plans…</div>
                                <!-- ⛔ Con lo que INCLUYE cada plan, no solo el
                                     nombre: nadie elige por "Basic" a secas, y
                                     el dato ya viene en la misma llamada. Sin
                                     esto se pide a ciegas y el socio recibe una
                                     peticion que su cliente no sabe si le sirve. -->
                                <div t-else="" class="ac-ins-btns">
                                    <button t-foreach="state.catalogo.plans" t-as="p" t-key="p.id"
                                            class="ac-btn-ghost-sm"
                                            t-att-disabled="state.pidiendo or yaPedido('plan', p.id)"
                                            t-on-click="pedirAlProveedor('plan', p.id)">
                                        <t t-esc="p.name"/>
                                        <t t-if="p.monthly_credits">
                                            · <t t-esc="p.monthly_credits"/> cr
                                        </t>
                                        <t t-if="yaPedido('plan', p.id)"> · requested</t>
                                    </button>
                                </div>
                                <div class="ac-ins-note" t-esc="notaProveedor"/>
                            </div>

                            <div t-if="state.pedirAbierto === 'topup'" class="ac-ins-new">
                                <div class="ac-ins-q">How many credits do you need?</div>
                                <div t-if="!state.catalogo" class="ac-ins-note">Reading the top-ups…</div>
                                <div t-else="" class="ac-ins-btns">
                                    <button t-foreach="state.catalogo.packs" t-as="k" t-key="k.id"
                                            class="ac-btn-ghost-sm"
                                            t-att-disabled="state.pidiendo or yaPedido('topup', k.id)"
                                            t-on-click="pedirAlProveedor('topup', k.id)">
                                        <t t-esc="k.credits"/> cr
                                        <t t-if="yaPedido('topup', k.id)"> · requested</t>
                                    </button>
                                </div>
                                <div class="ac-ins-note" t-esc="notaProveedor"/>
                            </div>

                            <div class="ac-ins-note">
                                Each person queries with their own ERP user, so they only see what they would see logging in there. Credits come out of the shared pot.
                            </div>
                            </t>
                        </t>

                        <!-- Su plan no da equipo: aqui SI tiene sentido ofrecerle
                             sentarse en la cuenta de su empresa. -->
                        <t t-else="">
                            <div class="ac-ins-q">Your plan is for one person</div>
                            <div class="ac-ins-note">If your company already pays for Aski, ask them for a seat instead of paying for a second plan.</div>
                            <div class="ac-ins-btns2">
                                <button class="ac-btn-primary-sm" t-on-click="pedirAsiento"
                                        t-att-disabled="state.seatBusy">
                                    <t t-if="state.seatBusy">Asking...</t>
                                    <t t-else="">Ask for a seat</t>
                                </button>
                            </div>
                            <div t-if="state.seatAsked" class="ac-ins-note ac-seat-done"
                                 t-esc="textoAsientoPedido"/>
                        </t>
                    </t>
                </div>
            </div>

            <div t-if="detailMsg" class="ac-sheet-wrap">
                <div class="ac-scrim" t-on-click="closeDetail"/>
                <div class="ac-sheet" role="dialog" aria-modal="true">
                    <t t-set="m" t-value="detailMsg"/>
                    <div class="ac-sheet-grip"/>
                    <div class="ac-sheet-head">
                        <span class="ac-sheet-title">Response details</span>
                        <button class="ac-sheet-close" title="Close" t-on-click="closeDetail">
                            <i class="fa fa-times"/>
                        </button>
                    </div>
                        <div class="ac-detail">
                            <!-- ⛔ `!= null` (suelto) a proposito: atrapa null Y
                                 undefined. Con `!== null` la casilla de filas se
                                 pintaba tambien cuando el campo no venia —una caja
                                 gris vacia al lado— y la de creditos enseñaba un
                                 guion. Una respuesta que no gasta creditos ni
                                 devuelve filas (una accion sobre el ERP, por
                                 ejemplo) no tiene nada que contar: la tira entera
                                 se va, en vez de dejar un hueco que parece roto. -->
                            <div t-if="m.credits != null or m.rows != null" class="ac-detail-stats">
                                <div t-if="m.credits != null" class="ac-stat">
                                    <i class="fa fa-bolt"/>
                                    <div><div class="ac-stat-v" t-esc="m.credits"/><div class="ac-stat-k">credits</div></div>
                                </div>
                                <div t-if="m.rows != null" class="ac-stat">
                                    <i class="fa fa-table"/>
                                    <div><div class="ac-stat-v" t-esc="m.rows"/><div class="ac-stat-k">rows</div></div>
                                </div>
                            </div>
                            <!-- Aqui vivia un segundo "Helpful? 👍👎", que se quito
                                 al subir los pulgares a la fila de la burbuja: este
                                 panel es INLINE (en la app y en la web el detalle es
                                 una hoja que TAPA el chat, y por eso alli si
                                 conviven), asi que los dos controles quedaban a la
                                 vista en la misma tarjeta, a 40 px uno del otro, y
                                 parecian un descuido. Lo que SI vive aqui es el POR
                                 QUE, lo unico que convierte un pulgar en algo
                                 accionable: la unica queja real de un cliente llego
                                 ESCRITA en el chat, nunca por el pulgar, asi que el
                                 panel de calidad no la veia. Solo se pide tras un 👎.
                                 La etiqueta ENVUELVE al textarea en vez de usar
                                 for/id: el widget puede estar montado dos veces a
                                 la vez (pantalla completa + burbuja del systray) y
                                 dos ids iguales dejarian la etiqueta ambigua.
                                 El contenido se pinta desde `fbInitial`, que NO
                                 cambia mientras el panel esta abierto: enlazarlo a
                                 `fbComment` haria que OWL reescribiera el texto en
                                 cada pulsacion y el cursor saltaria al final al
                                 corregir algo en mitad de la frase. -->
                            <!-- El voto TAMBIEN aqui, como en la hoja de Android y la
                                 de la web. No es un duplicado ocioso: la hoja TAPA el
                                 chat, asi que con ella abierta los pulgares pequenos de
                                 la burbuja quedan detras del scrim y no se pueden
                                 pulsar; sin estos habria que cerrar para cambiar el voto. -->
                            <div class="ac-detail-fb">
                                <span>Helpful?</span>
                                <button t-attf-class="ac-fb-btn {{ m.feedback === 'like' ? 'on' : '' }}"
                                        title="Helpful answer"
                                        t-att-aria-pressed="m.feedback === 'like' ? 'true' : 'false'"
                                        t-on-click="setFeedback(m, 'like')">
                                    <i class="fa fa-thumbs-o-up"/>
                                </button>
                                <button t-attf-class="ac-fb-btn {{ m.feedback === 'dislike' ? 'on' : '' }}"
                                        title="Unhelpful answer"
                                        t-att-aria-pressed="m.feedback === 'dislike' ? 'true' : 'false'"
                                        t-on-click="setFeedback(m, 'dislike')">
                                    <i class="fa fa-thumbs-o-down"/>
                                </button>
                            </div>
                            <label t-if="m.feedback === 'dislike'" class="ac-fb-why">
                                <span class="ac-fb-why-label">What went wrong? (optional)</span>
                                <textarea class="ac-fb-why-input" rows="3" maxlength="500"
                                          placeholder="E.g.: the total doesn't match what I see in my ERP"
                                          t-on-input="onFbCommentInput"
                                          t-esc="state.fbInitial"/>
                            </label>
                            <div t-if="m.feedback === 'dislike'" class="ac-fb-why-actions">
                                <button class="ac-fb-send" t-on-click="sendFeedbackComment(m)"
                                        t-att-disabled="state.fbSending || !state.fbComment.trim()">
                                    <t t-if="state.fbSending">Sending…</t>
                                    <t t-else="">Send</t>
                                </button>
                                <span t-if="state.fbSent" class="ac-fb-note ac-fb-ok">
                                    <i class="fa fa-check"/> Thanks. Now we know what to fix.
                                </span>
                                <span t-if="state.fbError" class="ac-fb-note ac-fb-bad">
                                    <i class="fa fa-exclamation-circle"/> Couldn't send your note. Please try again.
                                </span>
                            </div>
                            <div class="ac-acts">
                                <button t-if="m.backendId" class="ac-act"
                                        t-on-click="openRecords(m)"
                                        t-att-disabled="state.recordsBusy">
                                    <i class="fa fa-table"/> <span>See the records behind this</span>
                                </button>
                                <button class="ac-act" t-on-click="copyText(m)">
                                    <i class="fa fa-clone"/> <span>Copy text</span>
                                </button>
                                <button t-if="m.backendId" class="ac-act"
                                        t-on-click="openShare(m)">
                                    <i class="fa fa-link"/> <span>Share link</span>
                                </button>
                                <button class="ac-act" t-on-click="exportMessageDetail(m)"
                                        t-att-disabled="state.exporting">
                                    <i t-attf-class="fa {{ state.exporting ? 'fa-circle-o-notch fa-spin' : 'fa-file-pdf-o' }}"/>
                                    <span>Export this answer to PDF</span>
                                </button>
                                <button t-if="m.backendId" class="ac-act"
                                        t-on-click="exportXlsx(m)"
                                        t-att-disabled="state.xlsxBusy">
                                    <i t-attf-class="fa {{ state.xlsxBusy ? 'fa-circle-o-notch fa-spin' : 'fa-file-excel-o' }}"/>
                                    <span>Export to Excel</span>
                                </button>
                                <button t-if="m.backendId" class="ac-act"
                                        t-on-click="abrirAltaAviso(m)">
                                    <i class="fa fa-bell-o"/> <span>Alert me daily</span>
                                </button>
                                <button t-if="m.backendId" class="ac-act"
                                        t-on-click="openEmail(m)">
                                    <i class="fa fa-envelope-o"/> <span>Send by email</span>
                                </button>
                            </div>
                        </div>
                </div>
            </div>
        </div>
    </t>
</templates>
